import XCTest
@testable import material_icons_test

final class material_icons_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(material_icons_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
